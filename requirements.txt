# MCP Memory - Dependencies

# Core (required)
mcp>=1.0.0

# Embeddings (optional - installed by install.sh if chosen)
# Adds semantic search, +150MB RAM
# sentence-transformers>=2.2.0
# numpy>=1.24.0
